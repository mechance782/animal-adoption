<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Adoptions</title>
</head>
<body>
    <h1>Pet Adoptions</h1>
    <% if (data.quantity > 1) { %>
        <% data.petKind += "s" %>
    <% } %>
    <p>You want to adopt <%= data.quantity %> <%= data.color %> <%= data.petKind %>.</p>
    <a href="/adoptions">Adoptions</a>
</body>
</html>